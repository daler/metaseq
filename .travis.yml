language: python
install:
    - "sudo apt-get update && sudo apt-get -qq install inkscape python-scipy libatlas-dev"
    - "pip install -q cython scipy"
    - "pip install -r requirements.txt --use-mirrors"

script: ./.travis-test.sh

branches:
    only:
        - docs
